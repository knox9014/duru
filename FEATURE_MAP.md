# 두루 — 기능 대조표

> 기준: **Markdown Guide (Basic Syntax + Extended Syntax + Hacks) 전 항목**
> 검증: `node tier-check.js` — 65개 항목을 실제 파이프라인에 통과시켜 측정 (2026-08-24)
> 이 문서의 등급은 추측이 아니라 **측정값**이다.

---

## 0. 핵심 발견

**살아남는 것 ≠ 쓸 수 있는 것.**

측정 결과 **65개 항목 전부 왕복이 안정적이다. 소실 0.**
그러나 "열면 그대로 남는 것"과 "앱에서 만들 수 있는 것"은 다르다.

| 등급 | 뜻 | 사용자가 겪는 것 |
|---|---|---|
| **A. 만들 수 있음** | 툴바·단축키로 만들고 고친다 | 워드와 똑같이 동작 |
| **B. 고칠 수는 있음** | 글자로 다룰 수 있으나 만드는 버튼이 없다 | 손으로 쳐야 한다 |
| **C. 보존만** | 회색 상자로 얼어붙는다 | 편집도 삭제도 안 됨 |

---

## 1. A — 툴바·단축키로 만들 수 있다 (21항목)

| 항목 | 방법 |
|---|---|
| 제목 1~6 · 본문 · 코드 블록 | 드롭다운 |
| 굵게 · 기울임 · 취소선 · 인라인 코드 | 버튼 / `Ctrl+B` `Ctrl+I` |
| 단어 중간 강조 (`super***a***word`) | 선택 후 버튼 |
| 글머리표 · 번호 · 할 일 목록 | 버튼 |
| 중첩 목록 | `Tab` / `Shift+Tab` |
| 목록 항목 추가 | `Enter` |
| 인용 (중첩 포함) | 버튼 |
| 링크 (선택 / 커서만) | 버튼 → 대화상자 |
| 수평선 | 버튼 |
| 강제 개행 | `Shift+Enter` |
| Admonition (`> ⚠️ **Warning:**`) | 인용 + 굵게 조합 |
| 수동 목차 | 링크 조합 |

> Setext 제목(`===`)과 들여쓰기 코드 블록은 **열면 A로 바뀐다** —
> 각각 `#` 과 펜스로 정규화된다 (§3).

---

## 2. B — 고칠 수는 있으나 만들 버튼이 없다 (20항목)

편집기가 구조로 이해하므로 글자를 고칠 수 있다. 다만 **새로 만들려면 손으로 쳐야 한다.**

| 항목 | 언제 A가 되나 |
|---|---|
| **표** · 표 정렬 · 표 안 파이프(`\|`) | **3주차** |
| **이미지** · 링크 이미지 · 동영상 썸네일 | **3주차** |
| 링크 title (`[a](b "t")`) | 3주차 (링크 대화상자 확장) |
| 자동링크 `<https://x>` · 맨 URL | 이미 자동 |
| 목록 안 문단 · 목록 안 코드 블록 | v0.2 |
| 숫자 이스케이프 (`1968\.`) | — |
| 문자 이스케이프 (`\*` `\#`) | — |
| Heading ID (`{#id}`) | v0.2 검토 |
| 정의 목록 (`용어` / `: 정의`) | v0.2 검토 |
| 하이라이트 (`==강조==`) | v0.2 — §5 참조 |
| 아래첨자 (`H~2~O`) · 위첨자 (`X^2^`) | v0.2 검토 |
| 이모지 (유니코드 · `:rocket:`) | — |
| 특수기호 (`©` `π`) | — |

---

## 3. C — 회색 상자로 보존만 (18항목)

편집기가 모르는 것. **손댈 수 없지만 절대 사라지지 않는다.**

`<br>` · raw HTML(인라인·블록) · 각주 · 참조형 링크 정의 · frontmatter ·
표 안 `<br>` · 표 안 `<ul><li>` · `<mark>` · `<sub>` · `<ins>` ·
`<center>` · `<p style>` · `<font>` · `<img width>` · `<figure>` ·
`<a target="_blank">` · HTML 주석 · 주석 해킹 (`[note]: #`)

> **Markdown Guide 3장(Hacks)은 거의 전부 여기 들어간다.** 전부 HTML 우회법이기 때문이다.
> 밑줄만 3주차에 A로 승격한다 (§4).

---

## 4. 승격시키는 것 — 밑줄만 (3주차)

밑줄은 워드 사용자가 가장 많이 찾는 서식이다.
Markdown Guide 3.1은 `<ins>`를 제시하지만, **`<u>` 가 GitHub·VS Code·Obsidian
어디서나 렌더링**되므로 §30(상호운용)을 깨지 않는다.

- 구현 비용: 스키마에 마크 1개 + 직렬화 규칙 1줄
- **밑줄만 예외다.** 형광펜·글자색·글꼴·정렬은 승격하지 않는다.
  Markdown Guide가 이들을 전부 "Hacks"로 분류한 것과 같은 이유 —
  마크다운 문법이 아니라 HTML 우회법이고, 원본 계획서 §3.2
  "내용과 디자인 분리"를 정면으로 위반한다.

---

## 5. 정규화가 바꾸는 표기 (전부 가이드 권장 방향)

파일을 처음 열 때 한 번 통일된다. **뜻은 바뀌지 않는다.**

| 바뀌는 것 | 가이드 근거 |
|---|---|
| `__굵게__` → `**굵게**` | 4.3 — 단어 중간엔 `*`가 안전 |
| `___둘다___` → `***둘다***` | 4.3 |
| `*`/`+` 글머리표 → `-` | 4.5 — 한 목록에서 기호 통일 |
| `1)` → `1.` | 4.4 — `1.`이 호환성 좋음 |
| `***`/`___` 수평선 → `---` | 1.11 |
| Setext 제목 → ATX (`#`) | 1.1 권장 형태 |
| 들여쓰기 코드 블록 → 펜스 | 2.2 |
| `&#124;` → `\|` · `&copy;` → `©` | 동등, 더 읽기 쉬움 |
| `&nbsp;` → U+00A0 | 동등. **ASCII 공백이 아니라 코드 블록이 되지 않는다** |

**한 가지 가이드와 다른 선택:**

강제 개행을 `\` 로 저장한다. 가이드 1.4/4.6은 **공백 2개를 우선 권장**한다.

> 일부러 다르게 했다. 줄 끝 공백은 **눈에 보이지 않고 편집기·git 훅·린터가
> 조용히 지운다.** 그러면 개행이 사라지는데 아무도 모른다. `\` 는 보이고 살아남는다.
> 이 제품의 1번 원칙이 "뜻이 조용히 바뀌면 안 된다"이므로 이쪽을 택했다.

---

## 6. 없는 기능을 어떻게 알릴 것인가

**툴바에는 넣지 않는다. `서식` 메뉴 안에 흐린 글씨로 두고, 누르면 한 줄 설명.**

```
서식
  굵게              Ctrl+B
  기울임            Ctrl+I
  밑줄              Ctrl+U
  ────────────
  글자 색           ⓘ        ← 흐림. 누르면 아래 안내
  형광펜            ⓘ
  가운데 정렬       ⓘ
```

> 마크다운은 글자 색을 저장하지 않습니다.
> 문서를 어디서 열든 똑같이 보이게 하려고 일부러 뺐어요.
> 대신 **굵게** 나 인용을 써보세요.

**근거:** 찾다가 못 찾는 것보다, 있는데 왜 안 되는지 아는 편이 낫다.

---

## 7. 가이드 밖 문법 — 실무에서 더 흔한 것들

Markdown Guide 는 마크다운 전부를 다루지 않는다. 원본 계획서 §8 이 요구한
수식·Mermaid, 그리고 Obsidian·GitHub 확장이 빠져 있다. 따로 측정했다
(`beyond-guide.md`, `npm run beyond`).

**원인은 하나였다** — remark 가 모르는 "문장 안" 문법을 글자로 보고 escape 한다.
위키링크만의 문제가 아니라 13종이 같은 이유로 깨지고 있었다.

| 문법 | 전 | 후 |
|---|---|---|
| 위키링크 `[[x]]` · 별칭 · 임베드 `![[x]]` | `\[\[x]]` | ✅ 그대로 |
| **GitHub 알림 `> [!NOTE]`** | `> \[!NOTE]` | ✅ 그대로 |
| Obsidian 콜아웃 `> [!warning]` | 깨짐 | ✅ |
| 태그 `#프로젝트` | `\#프로젝트` | ✅ |
| 블록 링크 `[[문서#^id]]` | 깨짐 | ✅ |
| 수식 아래첨자 `$x_1$` | `$x\_1$` | ✅ |
| 약어 `*[HTML]: ...` | 깨짐 | ✅ |
| 인라인 각주 `^[내용]` | 깨짐 | ✅ |
| `[TOC]` · 확장 체크박스 `- [-]` | 깨짐 | ✅ |
| Mermaid · 블록 id `^abc` · `%%주석%%` | — | ✅ 원래 정상 |
| **LaTeX 간격 `\,` `\;`** | `,` | ❌ **남음** |

**GitHub 알림이 특히 컸다** — `> [!NOTE]` 는 GitHub 이 공식 지원하고 README 에
흔하다. 깨지면 남의 저장소 문서를 열었다 저장하는 순간 알림 상자가 사라진다.

### 어떻게 고쳤나

**"떼도 문서가 같으면 뗀다."** escape 를 제거한 글과 원래 글을 같은 방식으로
다시 써서 비교하고, 같을 때만 채택한다. 판정은 **줄 단위**로 하되 앞뒤 한 줄을
붙인다 — 줄 경계 효과(`===` 가 setext 제목 밑줄이 되는 것)까지 봐야 하기 때문이다.

- 처음엔 문서 전체를 매번 다시 파싱했다 → **5,300줄에서 46.7초.** 못 쓴다.
- 줄 단위로 바꿔 **598ms** 로 줄었다. 일반 문서는 4~23ms.
- **코드 블록·인라인 코드·frontmatter 는 건너뛴다.** 코드 안의 백슬래시를 떼면
  코드가 깨진다 (`const s = "a\nb";`). 애초에 그 안엔 escape 가 붙지
  않으므로 건드릴 이유도 없다.
- `\alpha` 처럼 **글자 앞의 백슬래시는 escape 가 아니라 내용**이다(LaTeX 명령).
  ASCII 구두점 앞의 것만 뗀다. 이걸 구분하지 않으면 수식 한 줄이 통째로 떨어진다.

### 남은 결함 — LaTeX 간격 명령 하나

```
원본:  \int_0^1 x^2 \, dx
저장:  \int_0^1 x^2 , dx      ← \, 의 백슬래시가 사라진다
```

`\,` 는 CommonMark 가 **파싱 단계에서** 유효한 escape(쉼표는 구두점)로 먹는다.
우리가 볼 때는 이미 사라진 뒤라 되돌릴 수 없다.

**해결: `$...$` 를 파서에게 불투명하게 만드는 것**(remark-math) 뿐이다. v0.2.
그때 수식이 B → A 로 승격되는 것과 같이 처리한다.

## 8. v0.2 후보

- **수식 `$...$` 지원** (§7) — remark-math. LaTeX 간격 명령 손실을 같이 해결
- 선택 서식 막대 (글자를 선택하면 뜨는 작은 팝업)
- 각주를 A로 승격
- Heading ID · 정의 목록 · 첨자를 A로 승격 검토
- 목차 패널
- 가져오기 시 손실 경고 (표 셀 병합 — 마크다운에 없음)
