# 두루 1주차 — 진짜 앱으로 만들기

> 지금 상태: 브라우저에서 도는 실험 화면. `out1.md` 를 코드에 박아넣고 읽는다.
> 1주차 후: 데스크톱 앱. 내 컴퓨터의 아무 `.md` 파일이나 열고 고치고 저장한다.

---

## 0. 절대 어기면 안 되는 것

### 0.1 변환 로직은 건드리지 않는다

`schema.js` · `convert.js` · `md.js` 는 **3일 검증을 통과한 코드다.**
1주차에서 이 세 파일의 **변환 규칙을 바꾸지 않는다.**
(`schema.js` 에 색 관련 추가는 없다. 여기엔 스타일이 없다.)

### 0.2 회귀 검사가 계속 통과해야 한다

작업 중 아무 때나, 그리고 **작업을 끝냈다고 말하기 전에 반드시**:

```bash
npm run exp2
```

기대 출력:

```
결과: ✅ 완전 일치 — 편집기를 통과해도 한 글자도 안 바뀜
G3 — 단어 하나 수정 후 바뀐 줄: 1
   ✅ 통과 — 고친 줄만 바뀜
```

**둘 중 하나라도 깨지면 그 자리에서 멈추고 원인을 찾는다.**
이게 이 제품의 존재 이유다. 다른 걸 다 만들어도 이게 깨지면 실패다.

`experiment.js` · `experiment2.js` · `check-loss.js` · `probe.js` ·
`duru-torture.md` · `word-habits.md` 는 **지우지 않는다.** 회귀 검사용이다.

### 0.3 색은 처음부터 CSS 변수로

CSS에 `#fff`, `#2a2724`, `rgb(...)` 같은 색을 **직접 쓰지 않는다.**
전부 `:root` 에 이름을 정의하고 `var(--...)` 로 참조한다.

```css
:root {
  --paper: #ffffff;      /* 문서 배경 */
  --canvas: #e9e5dc;     /* 문서 바깥 바탕 */
  --ink: #2a2724;        /* 본문 글자 */
  --ink-dim: #8a8378;    /* 흐린 글자 */
  --line: #d6d0c6;       /* 구분선·테두리 */
  --chrome: #f4f1ea;     /* 툴바·패널 배경 */
  --accent: #c2410c;     /* 강조 (선택·주요 버튼) */
  --raw-bg: #f4f1ea;     /* "원본 유지" 상자 배경 */
  --raw-line: #c9c9c9;   /* "원본 유지" 상자 테두리 */
}
```

이름이 더 필요하면 추가해도 된다. **다크 모드는 1주차에 만들지 않는다** —
이름만 제대로 붙여두면 나중에 한 벌 더 정의하면 끝이다.

### 0.4 모드(문서/작업실)는 1주차에 만들지 않는다

`MODE_PLAN.md` 는 2주차 이후다. 다만 1주차 결과물이 **작업실 모드의 모습**에
해당하므로, 좌측 트리·탭·`[문서][원본]` 전환을 만들 때
나중에 CSS로 숨길 수 있도록 **각각을 독립된 요소로** 만들어 둔다.

---

## 1. 준비된 것 (확인 완료, 다시 확인할 필요 없음)

- Rust 1.96.1 (`x86_64-pc-windows-msvc`) — 컴파일·링크 동작 확인함
- WebView2 151.x 설치됨
- Node v24.16.0, vite 8.2.2
- 기존 의존성: `unified` `remark-parse` `remark-gfm` `remark-frontmatter`
  `remark-stringify` `prosemirror-model` `prosemirror-view` `prosemirror-state`
  `prosemirror-keymap` `prosemirror-commands` `prosemirror-history`

---

## 2. 할 일

프론트엔드는 지금 있는 `duru/` 를 그대로 쓴다. Tauri를 그 위에 씌운다.

### W1 — Tauri 셸

- Tauri 2 를 `duru/src-tauri/` 로 추가. 프론트는 기존 vite (`duru/`)
- 창 제목 `두루`, 기본 크기 1180×760, 최소 720×480
- `npm run tauri dev` 로 데스크톱 창이 뜨고, 지금의 실험 화면이 그대로 보인다

**끝났다는 기준:** 데스크톱 창이 뜨고 편집기가 동작한다.

### W2 — 폴더 열기 + 파일 목록

- `파일 > 폴더 열기` (또는 첫 화면의 버튼) → 네이티브 폴더 선택 대화상자
- 고른 폴더를 재귀로 훑어 **`.md` 파일만** 목록에 넣는다
- 숨김 폴더(`.` 로 시작)와 `node_modules` 는 건너뛴다
- 좌측에 폴더 계층 트리로 표시. 폴더는 접고 펼 수 있다
- 마지막에 연 폴더 경로를 기억했다가 다음 실행에 자동으로 연다

**끝났다는 기준:** 이 저장소의 `duru/` 폴더를 열면 `.md` 파일들이 트리에 뜬다.

### W3 — 파일 열기 · 저장 · 최초 1회 정규화

이게 1주차의 핵심이다. **순서를 정확히 지킬 것.**

파일을 클릭했을 때:

```
1. 디스크에서 UTF-8 로 읽는다
2. 원본 문자열을 기억해 둔다            ← 3번 판단에 쓴다
3. parser.parse → 모르는 최상위 노드를 raw 로 보존 → writer.stringify
   ( = experiment.js 의 preserve() 와 같은 처리 )
4. 그 결과가 원본과 다르면  → 정규화가 일어난 것이다.
   그대로 디스크에 한 번 쓴다. 이것이 "최초 1회 정규화" (스펙 §2.1)
5. 정규화된 문자열을 toPM 으로 편집기에 싣는다
```

저장(`Ctrl+S`)할 때:

```
fromPM → writer.stringify → 디스크에 UTF-8 로 쓴다
```

지켜야 할 것:

- **개행 코드**: 읽은 파일이 CRLF 였으면 쓸 때도 CRLF 로 되돌린다.
  (remark 는 LF 로 출력한다. 읽을 때 어느 쪽이었는지 기록해 둘 것)
- 파일 끝 개행은 정확히 하나
- 저장 실패(권한 없음, 파일 사라짐)는 **조용히 넘기지 말고** 화면에 알린다
- `main.js` 의 `import source from './out1.md?raw'` 는 **삭제한다**

**끝났다는 기준:**
`duru-torture.md` 를 열고 아무것도 안 고치고 저장 → `git diff` 가 비어 있다.
(첫 열기의 정규화 저장은 예외 — 그때는 바뀐다. 두 번째부터 비어야 한다)

### W4 — 탭

- 파일을 클릭하면 새 탭으로 열린다. 이미 열려 있으면 그 탭으로 이동
- 탭 클릭으로 전환, `x` 로 닫기, `Ctrl+Tab` 으로 순환
- 저장 안 된 탭은 제목 옆에 점 표시
- 저장 안 된 탭을 닫으려 하면 물어본다 (저장 / 버리기 / 취소)

**끝났다는 기준:** 문서 3개를 열고 오가며 고쳐도 내용이 섞이지 않는다.

### W5 — 자동 저장 · 외부 변경 감지

- 마지막 입력 후 **5초** 동안 조용하면 자동 저장
- 앱 밖에서 파일이 바뀌면 감지한다
  - 그 탭에 **편집 중인 내용이 없으면** → 조용히 다시 읽는다
  - **편집 중이면** → 물어본다 ("파일이 밖에서 바뀌었습니다. 다시 읽을까요? / 내 것을 유지할까요?")
  - **자동 저장이 남의 변경을 덮어쓰면 안 된다.** 이게 이 항목의 존재 이유다

**끝났다는 기준:** 앱에서 파일을 열어둔 채 메모장으로 그 파일을 고치고 저장하면 앱이 반응한다.

### W6 — 최소한의 껍데기

- 메뉴: `파일`(폴더 열기 / 저장 / 닫기), `편집`(실행취소 / 다시실행 / 찾기)
- 단축키: `Ctrl+O` `Ctrl+S` `Ctrl+W` `Ctrl+Z` `Ctrl+Y` `Ctrl+F` `Ctrl+Tab`
- 문서 안 찾기·바꾸기
- 상태 표시: 저장됨 / 저장 중 / 저장 실패

**끝났다는 기준:** 위 단축키가 전부 동작한다.

---

## 3. 만들지 않는 것

- Git 관련 기능 전부 (4주차)
- 모드 전환, 다크 모드 (2주차 — `MODE_PLAN.md`)
- 표 삽입·편집 UI, 툴바 (3주차)
- 밑줄 지원 (3주차 — `FEATURE_MAP.md`)
- 워크스페이스 전체 검색, 목차, 명령 팔레트 (v0.2)
- 설정 화면 (2주차)
- 아이콘·폰트·색 다듬기. **1주차는 동작이 목표다.** 지금의 수수한 모습으로 충분하다

---

## 4. 다 끝났을 때 확인할 것

1. `npm run exp2` 통과 (§0.2)
2. `npm run loss` 통과
3. 앱에서 `duru-torture.md` 열고 → 저장 → 다시 저장 → `git diff` 비어 있음
4. 한 단어만 고치고 저장 → `git diff --stat` 이 `1 insertion(+), 1 deletion(-)`
5. 한글 입력이 정상 (커서 튐·글자 깨짐 없음)
6. 회색 "원본 유지" 상자가 여전히 보이고, 지워지지 않는다
7. CSS에 하드코딩된 색이 한 곳도 없다 (`grep -nE '#[0-9a-fA-F]{3,6}' *.css`)

**3·4번이 깨지면 나머지가 다 돼도 실패다.**

---

## 5. 막히면

- Tauri 설정에서 막히면: 파일 접근은 Rust 쪽 `command` 로 직접 구현해도 된다.
  플러그인을 꼭 쓸 필요 없다.
- 판단이 필요한 설계 결정이 나오면 **추측해서 진행하지 말고 멈추고 물어본다.**
  특히 변환 동작·저장 순서·정규화 시점에 관한 것.
