# 실험 #1 결과 — remark 왕복 + 원문 보존

날짜: 2026-08-23 · 대상: `duru-torture.md` (162줄, 항목 15종)
질문: **모르는 부분을 원문 그대로 떴다가 도로 넣으면 원본과 같아지는가?**

## 판정: 통과 (조건부)

| 게이트 | 결과 |
|---|---|
| G2 — 2차 저장부터 무변경 | ✅ 1차 출력 == 2차 출력 (완전 일치) |
| G1 — 소실 0 | ⚠️ 실질 소실 1건 (위키링크) |

## 원문 통째로 보존된 것 (최상위)

frontmatter 1 · raw HTML 5 · 참조식 링크 정의 2 · 각주 본문 2

`position` 으로 원본 줄 범위를 떠서 보관 → 그대로 되돌리는 방식이 **작동한다.**

## 표현만 바뀐 것 (의미 보존, 허용)

| 항목 | 변화 |
|---|---|
| 강제개행 | 공백2칸 2 + 역슬래시 1 → 역슬래시 3 (총 3 → 3) |
| 순서목록 | `1. 3. 7.` → `1. 2. 3.` (CommonMark 렌더링 동일) |
| `snake_case_변수` | `snake\_case\_변수` (이스케이프, 렌더링 동일) |
| 1차 정규화 | 120 / 162 줄 변경 — §2.1대로 최초 1회뿐 |

## 진짜 문제 1건

**위키링크** `[[architecture]]` → `\[\[architecture]]`

렌더링은 같지만 **Obsidian에서 링크로 인식되지 않는다.** G5(상호운용) 위반.

일반화하면: **remark 가 모르는 "인라인" 문법은 전부 이스케이프된다.**
각주 참조·참조식 링크는 remark 가 알아서 살렸지만, 표준 밖 문법은 못 산다.
현재 원문 보존은 최상위 블록에만 적용돼 인라인을 못 잡는다.

→ 해결: 인라인 확장을 micromark 확장으로 인식시켜 `raw` 노드로 보존.
   v0.1 범위 밖이나, **위키링크 든 파일을 손상시킨다는 건 알려진 제약으로 기록.**

## 남은 유보 — 이게 끝이 아니다

이번 실험은 **바닥(remark 왕복)만** 검증했다. ProseMirror 를 아직 안 붙였다.
진짜 소실은 편집기 스키마 경계에서 난다. 실험 #2 가 본게임이다.

## 재현

```
node experiment.js    # 왕복 + 안정성
node check-loss.js    # 소실 검사
```

---

# 실험 #2 결과 — 편집기(ProseMirror) 통과

질문: **편집기 문서로 바꿨다가 되돌려도 살아남는가?**
입력: `out1.md` (실험 #1의 정규화 완료본, 167줄)

## 판정: 통과

| 게이트 | 결과 |
|---|---|
| 무편집 왕복 | ✅ **완전 일치** — 한 글자도 안 바뀜 |
| G3 — 한 단어 수정 | ✅ **정확히 1줄만** 변경 |

## 진짜 구조로 들어갔는가 (전부 raw 로 뭉개지 않았는지)

```
text 112 · paragraph 38 · list_item 20 · table_cell 18 · heading 15
list 14 · table_row 7 · code_block 4 · hard_break 3 · hr 3 · table 2 · blockquote 2
────────────────────────────────────────────────────────
raw 10 · raw_inline 9        ← 손대지 않는 덩어리는 이만큼만
```

문서 대부분이 편집 가능한 구조로 들어갔고, 보존 덩어리는 19개뿐이다.
(frontmatter, HTML 블록, 각주 본문, 참조식 링크 정의, 문장 속 HTML·각주 참조)

## 코드 규모

`schema.js` + `convert.js` = **약 190줄**. 이게 "우리가 직접 짜는 부분"의 뼈대다.
예상(400~500줄)보다 작다. 나머지는 예외 처리로 늘어날 것.

## 남은 위험

1. **화면이 아직 없다.** 이번엔 문서 모델만 검증했다.
   실제 편집기(prosemirror-view)를 붙이면 **한글 조합·붙여넣기·커서**가 새 위험이다.
2. **위키링크** — 실험 #1의 문제 그대로. 미해결.
3. **마크 중첩 순서** — 되돌릴 때 고정 순서(link>strong>em>strike>code)로 감싼다.
   원본이 `[**a**](b)` 처럼 순서가 다르면 표현이 바뀐다.
   이 파일엔 해당 조합이 없어 안 걸렸다. **실제 문서에서는 나온다.**
4. 표 정렬·리스트 간격 등 더 험한 조합은 미검증.

## 종합

**§1(소실 0)의 기술적 타당성은 확인됐다.** 방식이 작동한다.
남은 큰 위험은 문서 처리가 아니라 **UI 쪽**으로 옮겨갔다.

---

# 실험 #3 결과 — 화면 붙이기 (prosemirror-view)

질문: **실제 편집기로 만들었을 때도 유지되는가? 한글은 되는가?**
`npm run dev` → http://localhost:5273 (좌: 편집기 / 우: 저장될 마크다운 실시간)

## 통과한 것

| 항목 | 결과 |
|---|---|
| 화면 렌더링 | ✅ 제목 15 · 목록 14 · 표 2 · 코드블록 4 · 보존덩어리 19 |
| 시작 상태 | ✅ 원본과 완전 일치 |
| 한글 텍스트 입력 | ✅ 입력 후 **1줄만** 변경 |
| 보존 덩어리 표시 | ✅ 회색 점선 상자 "원본 유지", 편집 불가 |

## 찾은 결함 → 고침

**보존 덩어리가 Backspace 로 삭제됐다.** (10개 → 9개, frontmatter 통째로 소실)

스펙 §2.3 은 "삭제도 불가"인데 안 지켜지고 있었다.

고침 — `filterTransaction` 으로 보존 덩어리 수가 줄어드는 변경을 거부 (3줄):

```js
const protectRaw = new Plugin({
  filterTransaction: (tr, state) =>
    !tr.docChanged || countRaw(tr.doc) >= countRaw(state.doc),
});
```

재검증: Backspace 3회 ✅ 유지 / **전체선택(Ctrl+A) 후 타이핑** ✅ 문서 안 날아감

## 남은 결함

1. **차단이 무디다.** Ctrl+A 후 타이핑 시 파괴는 막았지만, 친 글자가 문서 끝에
   엉뚱하게 붙었다. "정말 지울까요?" 확인 대화로 바꿔야 한다. (`ponytail:` 표시함)
2. ~~진짜 한글 조합(IME)은 검증 못 했다~~ → **2026-08-23 사람이 직접 확인, 정상.**
   커서 튐 없음, 글자 깨짐 없음.
3. 워드에서 붙여넣기 — 미검증.

## 사람이 직접 확인할 것

```
npm run dev   →  http://localhost:5273
```

- 한글을 길게 쳐본다 → 커서 튐, 글자 깨짐(`가ㄱ`), 조합 중 오른쪽 패널 이상 여부
- 표 칸 안에서 한글 입력 · 탭 이동
- 워드/한글에서 복사해 붙여넣기
- 회색 상자를 지워보려 시도

## 종합 — 0주차 관문 통과

| 실험 | 결과 |
|---|---|
| #1 원문 보존 (remark 왕복) | ✅ |
| #2 편집기 문서 왕복 | ✅ 한 단어 수정 = 1줄만 |
| #3 화면 + 한글 입력 | ✅ 결함 1개 발견·수정 |

**§1(아무것도 사라지지 않는다)이 기술적으로 성립한다. 1주차로 진행.**

남은 위험 3개 — 전부 알려진 상태이며 치명적이지 않다:
위키링크 이스케이프 · 강조 중첩 순서 고정 · 붙여넣기 미검증

---

# 전 문법 전수 점검 (2026-08-23)

`full-syntax.md` — CommonMark + GFM + frontmatter 의 mdast 노드 종류를 빠짐없이 넣고,
`duru-torture.md` 가 안 다루던 것까지 채운 시험지:

제목 1~6 · setext · 수평선 3형 · 강조 중첩 · 문장부호 옆 강조 ·
**번호 시작값(5부터)** · 느슨한/빡빡한 목록 · 목록 안 코드/표/인용/빈 항목 ·
할 일(x, X) · 인용 중첩·연속 · 펜스 메타 · 백틱4개 펜스 · 탭 들여쓰기 코드 ·
꺾쇠 목적지 · 빈 목적지 · **참조식 링크 3형(전체·축약·생략)** ·
**참조식 이미지** · 자동링크 · **GFM 자동링크(www., 맨 URL, 이메일)** ·
**링크 안 이미지** · 표 정렬 4종 · 표 안 서식 · 빈 셀 · 파이프 이스케이프 ·
강제 개행 2형 · 각주 2개 · 블록/인라인 HTML · 인라인 주석 ·
**문자 참조(&copy; &#65; &#x42;)** · 이스케이프 18종

## 찾은 버그 1개 — 고침

**링크 안에 이미지가 있으면 링크가 사라졌다.**

```
원본:  [![배지](./badge.png)](https://example.com)
결과:  ![배지](./badge.png)
```

README 의 빌드 배지가 정확히 이 모양이다. **남의 README 를 열었다 저장하면
배지 링크가 전부 죽는다.** 조용히 일어나므로 발견도 늦었을 것이다.

- 원인: `convert.js` 에서 이미지·강제개행·문장속보존 노드를 만들 때
  씌워진 마크(링크 등)를 넘기지 않았다
- 고침: `create(attrs, null, marks)` 로 마크를 넘김 (3곳)
- 재검증: 전 문법 왕복 **동일**, 기존 회귀 검사 2종 모두 통과

## 회귀 검사 명령

```
npm run exp2      편집기 왕복 + 한 단어 수정 = 1줄
npm run loss      소실 검사
npm run syntax    전 문법 왕복 (신규)
```
