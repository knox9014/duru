# 두루 2주차 (앞) — 워드처럼 보이게

> 1주차로 동작은 됐다. 그런데 **워드 같지 않다.**
> 이 작업의 목표는 기능이 아니라 **첫인상**이다.

---

## 0. 왜 이걸 지금 하나

스펙 §0 의 최우선 가치가 "워드처럼 쉬운 편집"인데, 지금 앱에는 **툴바가 아예 없다.**
워드의 정체성은 리본이다. 종이 배경만으로는 워드로 보이지 않는다.

또 완료 판정 G4("마크다운을 모르는 사람이 설명 없이 표를 만든다")는
툴바 없이는 시험 자체가 불가능하다.

---

## 1. 절대 어기면 안 되는 것

### 1.1 변환 코어를 건드리지 않는다

`schema.js` · `convert.js` · `md.js` · `normalize.js` 의 **변환 규칙을 바꾸지 않는다.**
툴바는 기존 스키마가 이미 아는 것만 만든다. 새 노드·새 마크를 추가하지 않는다.

> 예외 없음. 밑줄(`<u>`)은 3주차다. 지금 넣지 말 것.

### 1.2 회귀 검사가 계속 통과해야 한다

작업 끝났다고 말하기 전에 반드시:

```bash
npm run check
```

`exp2` · `syntax` · `e2e` 가 한 번에 돈다. 하나라도 깨지면 멈추고 원인을 찾는다.

### 1.3 색은 CSS 변수로

`style.css` 의 `:root` 밖에 색상 리터럴(`#fff` 등)을 쓰지 않는다.
이름이 더 필요하면 `:root` 에 추가한다. 확인:

```bash
npm run check && grep -nE '#[0-9a-fA-F]{3,8}' style.css
```

`:root` 블록 안에서만 나와야 한다.

### 1.4 **띄워서 눈으로 확인한다** ← 1주차의 교훈

1주차는 노드 스크립트 검사 18개를 전부 통과했는데, 정작 앱을 열어보니
찾기 바가 항상 떠 있고 빈 모달이 화면을 덮고 있었다.
`#findbar { display: flex }` 가 브라우저 기본 `[hidden] { display: none }` 을
특이도로 이겼기 때문이다. **스크립트로는 절대 안 잡히는 종류다.**

그래서 이번 작업은 **실제로 창을 띄워 화면을 확인한 것**이 완료 조건이다.
`npm run tauri dev` 로 띄우고, `full-syntax.md` 를 열어 §5 의 항목을 눈으로 확인한다.

---

## 2. T1 — 툴바

탭 바 아래, 편집 영역 위에 놓는다. 높이 **52px**, 배경 `var(--chrome)`.

```
[제목 ▾] │ [B] [I] [S] │ [• 목록] [1. 번호] [☑ 할 일] │ [" 인용] [🔗 링크] [</> 코드]
```

### 2.1 각 버튼이 하는 일

| 버튼 | 동작 | 스키마 |
|---|---|---|
| 제목 ▾ | 드롭다운: 본문 / 제목 1 / 제목 2 / 제목 3 / 제목 4 | `paragraph` ↔ `heading{level}` |
| B | 굵게 토글 | `strong` 마크 |
| I | 기울임 토글 | `em` 마크 |
| S | 취소선 토글 | `strike` 마크 |
| • 목록 | 글머리표 목록 토글 | `list{ordered:false}` |
| 1. 번호 | 번호 목록 토글 | `list{ordered:true}` |
| ☑ 할 일 | 체크박스 목록 토글 | `list` + `list_item{checked:false}` |
| " 인용 | 인용 토글 | `blockquote` |
| 🔗 링크 | 선택 영역에 링크. URL 을 기존 모달로 입력받는다 | `link` 마크 |
| `</>` 코드 | 인라인 코드 토글 | `code` 마크 |

**표·이미지는 넣지 않는다 (3주차).**

### 2.2 구현 방법

`prosemirror-commands` 의 `toggleMark` · `setBlockType` · `wrapIn` 과
`prosemirror-schema-list` 의 `wrapInList` · `liftListItem` · `sinkListItem` 을 쓴다.
목록 명령은 `prosemirror-schema-list` 가 필요하므로 **이 패키지 하나만 추가**한다.

> 우리 스키마의 목록 노드 이름은 `list` / `list_item` 이다 (`bullet_list` 아님).
> `wrapInList(schema.nodes.list, { ordered: true })` 처럼 attrs 를 넘긴다.

### 2.3 상태 반영 (중요 — 워드 느낌의 절반이다)

**커서 위치에 따라 버튼이 눌린 상태로 보여야 한다.**

- 굵은 글자 안에 커서 → `B` 버튼이 눌린 모양 (`.active`, 배경 `var(--hover)`)
- 제목 2 안에 커서 → 드롭다운이 "제목 2" 로 표시
- 목록 안 → 목록 버튼 활성

편집기 상태가 바뀔 때마다 갱신한다. 이게 없으면 버튼이 그냥 장식으로 느껴진다.

### 2.4 아이콘

**이모지를 쓰지 않는다.** 인라인 SVG 로 그린다 (16px, stroke 기반, 한 가지 스타일).
`B` `I` `S` 는 글자로 두되 각각 굵게·기울임·취소선을 적용해 보이게 한다.

### 2.5 단축키

`Ctrl+B` `Ctrl+I` 는 툴바 버튼과 **같은 명령을 부른다.** 두 경로가 갈라지면 안 된다.

---

## 3. T2 — 문서 타이포그래피

지금은 제목·문단이 전부 브라우저 기본값이라 **문서가 아니라 웹페이지처럼 보인다.**

### 3.1 본문

```css
.ProseMirror {
  font-size: 16px;
  line-height: 1.8;
  color: var(--ink);
}
.ProseMirror p { margin: 0 0 1em; }
```

폰트는 시스템 폰트로 간다 (오프라인 앱이라 웹폰트를 받을 수 없다).
본문용 스택: `'Malgun Gothic', 'Segoe UI', system-ui, sans-serif`.
**웹폰트 링크를 넣지 말 것** — 인터넷 없이도 똑같이 보여야 한다.

### 3.2 제목

워드의 제목 스타일처럼 **위 여백이 아래보다 넉넉해야** 한다. 브라우저 기본은 그 반대라 어색하다.

| | 크기 | 굵기 | 위 여백 | 아래 여백 |
|---|---|---|---|---|
| 제목 1 | 2.0em | 700 | 1.4em | 0.6em |
| 제목 2 | 1.5em | 700 | 1.3em | 0.5em |
| 제목 3 | 1.2em | 600 | 1.2em | 0.4em |
| 제목 4 | 1.05em | 600 | 1.1em | 0.4em |

문서 첫 요소의 위 여백은 0 으로 (`:first-child`).

### 3.3 나머지 요소

- **목록** — 들여쓰기와 항목 간격을 손본다. 중첩 목록도 확인할 것
- **할 일 목록** — 체크박스가 실제 네모로 보여야 한다. `list_item[data-checked]` 를 쓴다.
  체크 상태 표시만 하고, **클릭으로 토글하는 것은 3주차**
- **인용** — 왼쪽 세로선 + 흐린 글자
- **표** — 테두리·셀 여백·머리행 배경. 편집 기능은 3주차, **보기만** 정리
- **코드블록** — 이미 되어 있다. 여백만 확인
- **수평선** — 얇은 선

### 3.4 종이

이미 있다 (`#editor` 흰 배경, 800px, 가운데). 여기에만 추가:

- 종이에 옅은 그림자 — 바탕에서 떠 보이게
- 위아래 여백을 늘려 바탕이 보이게 (지금은 종이가 화면에 꽉 찬다)

### 3.5 "원본 유지" 상자

지금 스타일 유지. 다만 **본문 타이포그래피가 바뀌어도 눈에 띄게** 남아야 한다
(회색 점선, 고정폭 글꼴, 흐린 글자).

---

## 4. 만들지 않는 것

- 표 삽입·셀 편집, 이미지 넣기 (3주차)
- 밑줄 `<u>` (3주차)
- 모드 전환, 다크 모드 (2주차 뒤 — `MODE_PLAN.md`)
- 선택하면 뜨는 서식 막대 (v0.2)
- 애니메이션·전환 효과
- 새 의존성 (`prosemirror-schema-list` 하나만 예외)

---

## 5. 완료 조건 — 눈으로 확인할 것

`npm run tauri dev` 로 띄우고 `full-syntax.md` 를 연다.

1. 툴바가 탭 아래 보이고, 버튼이 눌린다
2. 문장을 선택하고 `B` → 굵어진다. 다시 → 풀린다
3. **커서를 굵은 글자 안에 두면 `B` 버튼이 눌린 모양이 된다**
4. 커서를 제목 위에 두면 드롭다운이 "제목 2" 등으로 바뀐다
5. 드롭다운으로 본문 → 제목 1 로 바꿔진다
6. 목록·번호·인용 버튼이 동작한다
7. 링크 버튼 → 모달로 URL 입력 → 링크가 걸린다
8. 제목이 문서처럼 보인다 (위 여백이 아래보다 넓다)
9. 회색 점선 "원본 유지" 상자가 여전히 눈에 띈다
10. 종이가 바탕 위에 떠 보인다
11. **한글 입력이 여전히 정상이다** (툴바 추가로 포커스가 깨지지 않았는지)

그리고:

12. `npm run check` 통과
13. `style.css` 의 `:root` 밖에 색상 리터럴 0개
14. **저장 후 `git diff` 가 깨끗하다** — 툴바로 서식을 넣지 않은 문서는
    열고 저장해도 파일이 안 바뀌어야 한다

**14번이 제일 중요하다.** 툴바가 편집기 상태를 잘못 건드리면 여기서 드러난다.

---

## 6. 막히면

우리 스키마는 표준 ProseMirror 예제와 노드 이름이 다르다 (`list`/`list_item`,
`hr`, `strike`). 예제 코드를 그대로 붙여넣지 말고 `schema.js` 를 먼저 읽을 것.

변환 동작·저장 순서에 관한 판단이 필요하면 **추측하지 말고 멈추고 물어본다.**
