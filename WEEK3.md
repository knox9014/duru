# 두루 3주차 — 표 · 이미지 · 밑줄

> 이 주차가 제품의 핵심 가치다.
> 완료 판정 **G4**("마크다운을 모르는 사람이 설명 없이 표를 만든다")를
> 지금까지는 **시험할 수조차 없었다.** 표 삽입이 없었기 때문이다.

---

## 0. 왜 표가 제일 중요한가

```
| 항목 | 값 |
| --- | --- |
| 가 | 나 |
```

이걸 손으로 치라고 하면 타깃 사용자(기획자·조교·테크라이터)는 그 자리에서 앱을 닫는다.
**마크다운에서 가장 쓰기 힘든 것이 표이고, 워드에서 가장 쉬운 것도 표다.**
그 격차가 이 제품의 존재 이유다.

---

## 1. 절대 어기면 안 되는 것

### 1.1 변환 코어 — 이번 주는 예외가 하나 있다

`schema.js` · `convert.js` · `md.js` · `normalize.js` 는 원칙적으로 건드리지 않는다.

**단 하나의 예외: 밑줄(§4).** 스키마에 마크 1개를 추가한다.
그 외의 변환 규칙 변경은 없다. 표·이미지는 **이미 스키마가 아는 것**이므로
새 노드를 만들 필요가 없다 (`table` · `table_row` · `table_cell` · `image`).

### 1.2 회귀 검사

```bash
npm run check
```

시험지 5벌이 돈다. **밑줄을 넣은 뒤에는 특히 조심할 것** — 변환 코어를
건드리는 유일한 작업이라 여기서 깨지면 바로 알아야 한다.

추가로 표 작업 후에는 `npm run tiers` 로 문법 등급이 안 내려갔는지 본다.

### 1.3 마크다운이 못 하는 것을 만들지 않는다

표에서 **셀 병합은 마크다운에 없다.** 만들 방법을 제공하지 않는다.
셀 안에 제목·목록·인용·수평선·이미지도 넣을 수 없다 (Markdown Guide 2.1).
**할 수 없는 것을 UI 에 두지 않는다** — 누르면 안 되는 버튼이 최악이다.

### 1.4 띄워서 눈으로 확인한다

2주차의 버그 9건이 전부 "회귀 검사 통과" 상태에서 나왔다.
표는 UI 비중이 크므로 더 그렇다. 브라우저 도구로 실제 화면을 확인할 것
(`window.__duru.openText` 로 Tauri 없이 문서를 띄울 수 있다).

---

## 2. T1 — 표

### 2.1 삽입

툴바 `표` 버튼 → 크기 고르기 격자가 뜬다.

```
┌─────────────────┐
│ ▦ ▦ ▦ ▦ ▦ │
│ ▦ ▦ ▦ ▦ ▦ │   칸에 마우스를 올리면 그만큼 칠해지고
│ ▦ ▦ ▦ ▦ ▦ │   아래에 "3 × 2" 로 표시된다
│ ▦ ▦ ▦ ▦ ▦ │
│      3 × 2      │
└─────────────────┘
```

- 최대 8열 × 8행. 그 이상은 만든 뒤 행·열 추가로 늘린다
- 만들어지는 표는 **머리행 1줄 + 본문 1줄 이상**. 마크다운 표는 머리행이 필수다
- 삽입 위치는 **현재 문단 뒤** (수평선과 같은 규칙 — 단어 중간을 쪼개지 않는다)
- 삽입 직후 커서는 **첫 번째 머리 셀**

### 2.2 셀 이동 — 워드와 같게

| 키 | 동작 |
|---|---|
| `Tab` | 다음 셀. 행 끝이면 다음 행 첫 셀 |
| **마지막 셀에서 `Tab`** | **새 행을 추가하고 그 첫 셀로** ← 워드의 핵심 동작 |
| `Shift+Tab` | 이전 셀 |
| `↑` `↓` | 위·아래 행의 같은 열 |
| `←` `→` | 셀 안에서 글자 이동. 끝에 닿으면 옆 셀로 |

> **주의:** 목록의 `Tab`(들여쓰기)과 충돌한다. 표 안에서는 표 동작이 우선이다.
> 키맵 순서로 해결할 것 — 표 키맵을 목록 키맵보다 앞에 둔다.

### 2.3 행·열 편집

표 안에 커서가 있으면 **표 바로 위에 작은 도구막대**가 뜬다.

```
   [＋행] [－행] [＋열] [－열] │ [◧ 왼쪽] [◫ 가운데] [◨ 오른쪽] │ [🗑 표 삭제]
┌──────────┬──────────┐
│ 머리 1    │ 머리 2    │
```

- `＋행` 은 **현재 행 아래**에, `＋열` 은 **현재 열 오른쪽**에 넣는다
- `－행` 으로 머리행은 지울 수 없다 (마크다운 표에 필수)
- 행이 1줄(머리행만) 남으면 `－행` 비활성
- 정렬 3개는 **현재 열**에 적용. 마크다운의 `:---` `:---:` `---:` 로 저장된다
- 도구막대는 표 밖으로 커서가 나가면 사라진다

### 2.4 셀 안에서 Enter — 막는다

마크다운 표 셀에는 **줄바꿈을 넣을 수 없다.**
`Enter` 를 누르면 아무 일도 일어나지 않게 하고, 상태바에 한 줄 띄운다:

> 표 안에서는 줄을 바꿀 수 없습니다. 마크다운 표의 제약입니다.

**조용한 무반응은 고장으로 보인다.** 반드시 알린다.
(`<br>` 우회는 v0.2 — 지금 넣으면 파일에 HTML 이 섞인다)

### 2.5 표는 이미 왕복이 검증돼 있다

`schema.js` 의 `table` / `table_row` / `table_cell` 과 정렬 속성은
0주차부터 시험지에 들어 있었고 지금도 통과한다.
**이번 주 작업은 순수하게 UI 다.** 변환 쪽은 손대지 않는다.

---

## 3. T2 — 이미지

### 3.1 넣는 방법 세 가지

1. 툴바 `이미지` 버튼 → 파일 선택 대화상자
2. **붙여넣기** (`Ctrl+V` 로 클립보드 이미지)
3. **드래그 & 드롭**

세 경로 모두 같은 처리로 모인다.

### 3.2 파일을 어디에 두나

**문서가 있는 폴더 밑 `assets/` 에 복사한다.**

```
프로젝트/
├─ 기획서.md
└─ assets/
   └─ 화면.png
```

- 문서에는 **상대 경로**로 쓴다: `![](./assets/화면.png)`
- 절대 경로를 쓰지 않는다 — 폴더째 옮기면 깨진다
- 같은 이름이 있으면 `화면-2.png` 처럼 숫자를 붙인다
- 붙여넣기로 들어온 이름 없는 이미지는 `붙여넣기-20260824-153012.png`
- `assets/` 가 없으면 만든다

### 3.3 화면에서

- 편집기 안에 실제 이미지로 보인다 (폭은 종이 폭에 맞춰 축소)
- 파일이 없으면 회색 상자에 파일명과 "그림을 찾을 수 없습니다"
- 클릭하면 선택되고 `Delete` 로 지운다. **파일은 지우지 않는다**
  (다른 문서가 쓰고 있을 수 있다)

### 3.4 크기 조절은 없다

마크다운 이미지 문법에 크기가 없다 ([FEATURE_MAP](FEATURE_MAP.md) §3).
`<img width>` 는 HTML 우회법이라 C등급이다. **v0.1에서 만들지 않는다.**
필요하면 `서식` 메뉴 안내(§6)와 같은 방식으로 알린다.

---

## 3.5 T4 — 붙여넣기 (측정 결과 추가됨, 2026-08-24)

**타깃이 워드에서 오는 사람인데 붙여넣기가 전부 평문이 된다.**
`paste-check.js` 로 실측했다 (워드가 실제로 내보내는 HTML 을 그대로 넣어 확인).



| 출처 | 지금 |
|---|---|
| 워드 | 제목·굵게·기울임·링크 전부 소실. **표는 셀이 문단으로 흩어짐** |
| 웹페이지 | 전부 소실. 표는 `가나12` 로 뭉개짐 |
| 구글 문서 | 전부 소실 |
| 글자만 | ✅ 정상 |
| **마크다운 텍스트** | **`# 제목` → `\# 제목`, `**굵게**` → `\*\*굵게\*\*`** |

**좋은 소식 하나:** HTML 쓰레기(`mso-`, `<span style=…>`)는 들어오지 않는다.
파일은 깨끗하게 유지된다 — §30(상호운용)은 안 깨진다.

### 원인

`schema.js` 에 **`parseDOM` 규칙이 하나도 없다.** `toDOM`(화면에 그리기)만 있고
그 반대 방향이 없어서, 붙여넣은 HTML 을 우리 노드로 바꿀 방법이 없다.
ProseMirror 는 남는 글자만 문단으로 넣는다.

### 할 일

**T4-a. `parseDOM` 규칙 추가** — 표 작업과 같은 주에 하는 것이 맞다.
표 붙여넣기가 여기서 같이 해결되기 때문이다.

- 노드: `heading`(h1~h6) · `paragraph`(p) · `blockquote` · `code_block`(pre) ·
  `hr` · `list`(ul/ol) · `list_item`(li) · `table`/`table_row`/`table_cell` ·
  `image`(img) · `hard_break`(br)
- 마크: `strong`(b·strong·font-weight≥600) · `em`(i·em·font-style:italic) ·
  `strike`(s·del) · `code`(code) · `link`(a[href])
- **워드 특유의 것을 버린다:** `class^=Mso` · `mso-*` 스타일 · `<o:p>` ·
  `<![if !supportLists]>` 조건부 주석 · 목록 흉내용 `<span>` 안 하이픈
- **스키마를 건드리므로 회귀 검사 필수.** 밑줄(§4)과 같은 취급.

**T4-b. 마크다운 텍스트 붙여넣기 — 결정됨 (2026-08-24)**

| 키 | 동작 |
|---|---|
| `Ctrl+V` | **HTML 이 있으면** 서식을 살려 넣는다 (T4-a 가 처리) |
| | **평문뿐이면** 글자 그대로 넣는다. 마크다운 기호는 escape 된다 |
| `Ctrl+Shift+V` | **평문을 마크다운으로 해석해서** 넣는다 |

구현: `Ctrl+Shift+V` 는 클립보드 평문을 `parser.parse` → `toPM` 으로 통과시켜
문서 조각을 만들고 커서 자리에 끼워 넣는다. 파일에서 문서를 열 때 쓰는
경로와 **같은 코드**를 쓴다 — 새 변환 규칙을 만들지 않는다.

- 붙여넣은 조각도 `raw` 보존이 그대로 적용된다 (모르는 것은 원문째)
- 붙인 뒤 저장했을 때 회귀 검사가 통과해야 한다

### ⚠ 관례와 반대라는 점

거의 모든 앱에서 `Ctrl+Shift+V` 는 **"서식 없이 붙여넣기"** 다
(워드 · 크롬 · 구글 문서 · Obsidian · Typora). 우리는 그 키를
**"마크다운으로 해석"** 에 쓰기로 했으므로 의미가 정반대다.

의도적인 선택이다 — 타깃이 비개발자라 `Ctrl+V` 가 조용히 `*` 를 강조로
바꾸면 놀란다. 기본을 "보이는 그대로"에 두고, 마크다운 해석은
명시적으로 요청할 때만 한다.

**써보고 헷갈리면 두 키의 동작을 맞바꾸면 된다** (Obsidian 방식).
바꾸는 곳은 붙여넣기 처리 한 군데뿐이다.

### 완료 조건 (§6 에 추가)

15. 마크다운 텍스트를 `Ctrl+V` → 글자 그대로 (역슬래시가 붙어 escape 됨)
16. 같은 것을 `Ctrl+Shift+V` → 진짜 제목·굵게·목록으로 들어간다
17. 워드에서 복사한 것을 `Ctrl+V` → 제목·굵게·목록·표가 살아 있다
18. 붙여넣은 뒤 저장 → `npm run check` 통과

---

## 4. T3 — 밑줄

**이번 주 유일하게 변환 코어를 건드리는 작업이다.**

- `schema.js` 의 marks 에 `underline` 추가, `toDOM: () => ['u', 0]`
- `convert.js` — mdast 쪽에는 대응 노드가 없다. `<u>...</u>` 를
  **문장 안 raw 로 읽고 쓰는** 규칙을 넣는다
- 툴바 `U` 버튼 + `Ctrl+U`
- 직렬화 결과는 `<u>밑줄</u>`

**왜 `<u>` 인가:** Markdown Guide 3.1 은 `<ins>` 를 제시하지만,
`<u>` 가 GitHub · VS Code · Obsidian 어디서나 렌더링된다 (FEATURE_MAP §4).

**주의:** 이미 `<u>` 가 들어 있는 문서를 열면 지금은 **C등급(회색 상자)** 이다.
밑줄을 넣으면 그게 **편집 가능한 마크**로 바뀐다. 회귀 검사가 이 변화를
잡아낼 것이다 — 시험지의 기대값을 같이 고쳐야 한다.

---

## 5. 만들지 않는 것

- 셀 병합 (마크다운에 없다)
- (T4-b 는 결정됐다 — Ctrl+Shift+V. §3.5 참조)
- 셀 안 줄바꿈 `<br>` (v0.2)
- 셀 안 목록·제목·인용 (마크다운에 없다)
- 이미지 크기 조절·정렬·캡션 (전부 HTML 우회법)
- 표를 그림처럼 끌어서 크기 조절
- 형광펜·글자색·글꼴 (FEATURE_MAP §4 — 승격 대상 아님)

---

## 6. 완료 조건

### 스크립트

1. `npm run check` 통과 (시험지 5벌)
2. `npm run tiers` — 등급이 내려간 항목이 없다
3. 표가 든 문서를 열고 저장 → `git diff` 비어 있음
4. 표 안에서 글자 하나만 고치고 저장 → 그 줄만 바뀜

### 눈으로

5. 툴바 `표` → 격자에서 3×2 선택 → 표가 생기고 커서가 첫 셀에 있다
6. `Tab` 으로 셀을 끝까지 이동 → **마지막 셀에서 한 번 더 누르면 새 행이 생긴다**
7. 표 안에 커서를 두면 위에 도구막대가 뜨고, 밖으로 나가면 사라진다
8. 열 정렬 3종이 적용되고 저장된 마크다운에 `:---:` 로 남는다
9. 셀에서 `Enter` → 아무 일도 안 일어나고 상태바에 안내가 뜬다
10. 이미지 붙여넣기 → `assets/` 에 파일이 생기고 화면에 그림이 보인다
11. 문서를 다른 폴더로 옮겨도 (폴더째) 그림이 보인다
12. `Ctrl+U` 로 밑줄이 걸리고 저장하면 `<u>...</u>` 가 된다
13. 한글 입력이 표 안에서도 정상이다

### 사람으로 — **이게 진짜 판정이다**

14. **마크다운을 모르는 사람에게 아무 설명 없이 "표 하나 만들어보세요" 라고 한다.**
    해내면 G4 통과. 헤매면 어디서 막혔는지 적는다.

> 14번이 이 프로젝트의 완료 판정 G4 다. 1~13이 다 통과해도 14가 안 되면
> 제품 목표를 못 맞춘 것이다.

---

## 7. 순서

```
T1 표         ← 제일 크다. 여기부터
T4-a 붙여넣기  ← parseDOM. 표 붙여넣기가 같이 해결된다
T3 밑줄       ← 작다. 변환 코어를 건드리므로 표와 섞지 않는다
T2 이미지     ← 파일 시스템이 얽혀 있다. 마지막
```

**표와 밑줄을 같은 커밋에 넣지 말 것.** 회귀 검사가 깨졌을 때
어느 쪽 때문인지 알 수 없게 된다.

---

## 8. 막히면

- 표 키맵과 목록 키맵의 `Tab` 충돌 → 표 키맵을 앞에 두고, 표 밖에서는
  `false` 를 반환해 목록 키맵으로 넘긴다
- 도구막대 위치 계산 → `view.coordsAtPos` 로 표의 화면 좌표를 얻는다
- 변환 동작·저장 순서에 관한 판단이 필요하면 **추측하지 말고 멈추고 물어본다**
