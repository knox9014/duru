# 두루 — 모드 전환 계획

> 대상: 구현을 맡을 에이전트.
> 이 문서는 **추측 없이 그대로 실행 가능한 수준**으로 쓰였다.
> 애매한 부분이 있으면 구현하지 말고 질문할 것.

---

## 0. 한 줄 원칙 (이걸 어기면 전부 실패)

> **모드는 boolean 하나다. 편집기 코드에는 모드 분기가 없다.**

모드가 결정하는 것은 **무엇을 감추고 무엇을 크게 그릴지**뿐이다.
문서 데이터, 편집 동작, 저장 방식, 단축키는 두 모드가 **완전히 동일한 코드**를 쓴다.

구현 방법도 고정한다:

```
<div id="app" data-mode="doc">     ← 또는 data-mode="studio"
```

`data-mode` 속성 하나만 바꾼다. 나머지는 **전부 CSS**로 처리한다.

```css
#app[data-mode="doc"] .studio-only { display: none; }
#app[data-mode="studio"] .doc-only { display: none; }
```

JS 분기(`if (mode === 'studio')`)는 아래 §3의 **동작 차이 2개에서만** 허용한다.
그 외의 곳에 `if (mode` 가 등장하면 잘못 구현한 것이다.

---

## 1. 이름

| 쓰는 이름      | 코드값      | 안 쓰는 이름           |
| ---------- | -------- | ----------------- |
| **문서 모드**  | `doc`    | ~~일반인용, 초보자, 쉬움~~ |
| **작업실 모드** | `studio` | ~~전문가용, 고급, 개발자~~ |

사람을 등급 매기지 않는다. 작업 공간을 고르게 한다.

---

## 2. 무엇이 같은가 (건드리면 안 되는 것)

두 모드에서 **바이트 단위로 동일**하게 동작해야 한다:

- 편집기 본체 (ProseMirror), `schema.js` · `convert.js` · `md.js`
- "원본 유지" 덩어리의 보존·삭제 차단 동작 (스펙 §2.3)
- 파일 열기 · 저장 · 자동 저장 · 최초 1회 정규화 (스펙 §2.1)
- 모든 단축키
- 저장되는 `.md` 파일의 내용

> **검증:** 같은 문서를 두 모드에서 각각 열고 저장했을 때
> `git diff` 가 비어야 한다. 다르면 모드가 데이터에 샌 것이다.

---

## 3. 무엇이 다른가

### 3.1 v0.1에 넣는 차이 (딱 4개)

| # | 요소             | 문서 모드 (`doc`)              | 작업실 모드 (`studio`) | 구현                              |
| - | -------------- | -------------------------- | ----------------- | ------------------------------- |
| 1 | 좌측 목록          | 1단 평평한 목록, 폭 190px         | 폴더 계층 트리, 폭 224px | CSS + 트리 컴포넌트에 `studio-only`    |
| 2 | 탭 바            | 숨김                         | 표시                | CSS (`.tabbar` 에 `studio-only`) |
| 3 | 문서 배경          | 회색 바탕 위 흰 종이(그림자, 폭 660px) | 배경 없이 편집 영역 전체 폭  | CSS만                            |
| 4 | `[문서] [원본]` 전환 | 숨김 (항상 문서)                 | 표시                | CSS (`studio-only`)             |

**JS 분기가 허용되는 유일한 지점 2곳:**

- (a) 좌측 목록에서 파일을 클릭했을 때
  - `doc`: 현재 탭을 **교체**한다 (새 탭을 만들지 않는다 — 워드처럼 한 번에 한 문서)
  - `studio`: 새 탭을 **추가**한다
- (b) `[원본]` 뷰가 켜진 상태에서 `doc` 로 전환되면
  - 강제로 `[문서]` 뷰로 되돌린다 (숨겨진 UI가 켜져 있는 상태 방지)

### 3.2 v0.2로 미루는 차이 (지금 만들지 말 것)

- 우측 패널(변경 내용 / 목차) — 작업실 모드 전용
- 상태바 정보량 차이 (브랜치·변경 수·단어 수)
- 툴바 밀도 차이 (문서 모드 58px 라벨형 / 작업실 모드 40px 아이콘형)
- "원본 유지" 상자 라벨 문구 차이

> **왜 미루나:** v0.1에서 두 모드의 차이가 커질수록 만들 것과 시험할 것이 두 배가 된다.
> 위 4개만으로도 두 모드는 확실히 다르게 느껴진다. 더 필요한지는 써보고 정한다.

---

## 4. 첫 실행 화면

설정 파일이 **없을 때만** 뜬다. 앱 전체를 덮는 한 장.

```
┌──────────────────────────────────────────────┐
│                                              │
│                   두루                        │
│         어떻게 쓰실 건가요?                    │
│                                              │
│   ┌───────────────┐   ┌───────────────┐      │
│   │   문서 모드    │   │  작업실 모드   │      │
│   │               │   │               │      │
│   │  [미리보기]    │   │  [미리보기]    │      │
│   │               │   │               │      │
│   │ 한 번에 한 문서 │   │ 여러 문서를    │      │
│   │ 씩, 종이처럼   │   │ 오가며 작업    │      │
│   └───────────────┘   └───────────────┘      │
│                                              │
│        나중에 설정에서 바꿀 수 있어요           │
└──────────────────────────────────────────────┘
```

규칙:

- 두 카드는 **동등하게** 그린다. 한쪽에만 "추천" 배지를 달지 않는다.
- 미리보기는 각 모드의 화면을 축소한 그림 (v0.1에서는 정적 이미지도 허용).
- 맨 아래 안내 문구는 **반드시** 넣는다 — 되돌릴 수 있다는 걸 알아야 부담 없이 고른다.
- 건너뛰기 버튼은 없다. 대신 `Esc` 를 누르면 `doc` 으로 정하고 닫는다.
- 이 화면은 **딱 한 번만** 뜬다. 이후엔 절대 다시 뜨지 않는다.

---

## 5. 설정에서 바꾸기

`파일 > 설정` (또는 `Ctrl+,`) 안의 첫 항목.

```
모드     ( ● 문서    ○ 작업실 )
         한 번에 한 문서씩 종이처럼 봅니다.
```

- 라디오 두 개. 고르는 즉시 적용된다 (확인 버튼 없음, 앱 재시작 없음).
- 선택된 쪽의 설명 한 줄이 아래에 바뀌어 나온다.
- 전환 시 **열려 있는 문서와 편집 중인 내용은 그대로 유지**된다.
  (`studio` → `doc` 으로 갈 때 탭이 여러 개면 **현재 탭만 남기고 나머지는 닫는다.
  닫기 전에 저장 안 된 문서가 있으면 저장 여부를 묻는다.**)

---

## 6. 어디에 저장하나

**워크스페이스가 아니라 앱 전역 설정이다.** 모드는 사람의 취향이지 프로젝트의 속성이 아니다.

- 위치: Tauri 앱 설정 디렉터리의 `settings.json`
  (원본 계획서 §29의 `.mdide/settings.json` 은 워크스페이스용이므로 여기 쓰지 않는다)
- 내용:

```json
{ "mode": "doc" }
```

- 파일이 없거나 읽기에 실패하면 → `doc` 으로 간주하고 **첫 실행 화면을 띄운다**
- `mode` 값이 `doc` / `studio` 둘 다 아니면 → `doc` 으로 고치고 저장, 첫 실행 화면은 **띄우지 않는다**

---

## 7. 구현 순서 (각 단계는 독립적으로 위임 가능)

| 단계 | 할 일                                                        | 끝났다는 기준                            |
| -- | ---------------------------------------------------------- | ---------------------------------- |
| M1 | `settings.json` 읽기/쓰기 + `mode` 상태 + 루트에 `data-mode` 반영     | 콘솔에서 값을 바꾸면 속성이 바뀌고, 앱을 껐다 켜도 유지된다 |
| M2 | `.doc-only` / `.studio-only` CSS 두 줄 + §3.1의 차이 4개에 클래스 부착 | `data-mode` 를 손으로 바꾸면 화면 4곳이 바뀐다   |
| M3 | §3.1의 JS 분기 2개 (탭 교체 / 원본뷰 복귀)                             | 문서 모드에서 파일을 여러 개 눌러도 탭이 하나다        |
| M4 | 설정 화면의 라디오 두 개                                             | 즉시 반영되고, 탭 정리 시 저장 여부를 묻는다         |
| M5 | 첫 실행 화면                                                    | 설정 파일을 지우고 실행하면 뜨고, 고르면 다시 안 뜬다    |

**M1 → M2 → M3 순서를 지킬 것.** M2 부터는 M1 이 있어야 확인이 가능하다.
M4·M5 는 M3 이후라면 순서를 바꿔도 된다.

---

## 8. 하지 않는 것

- 모드에 따라 **기능을 잠그지 않는다.** 문서 모드에서도 표·코드·Git은 전부 쓸 수 있다.
  감추는 것은 *배치와 밀도*이지 *능력*이 아니다.
- 세 번째 모드를 만들지 않는다.
- 모드를 워크스페이스별로 저장하지 않는다.
- 첫 실행 화면에서 이름·이메일·용도 같은 걸 묻지 않는다. 질문은 하나뿐이다.
- 모드 전환에 애니메이션을 넣지 않는다 (즉시 전환).

---

## 9. 완료 판정

1. 설정 파일을 지우고 실행 → 첫 실행 화면이 뜬다
2. 문서 모드 선택 → 종이 배경, 탭 없음, 1단 목록, `[원본]` 버튼 없음
3. 설정에서 작업실로 전환 → 즉시 트리·탭·`[원본]` 이 나타나고, 열려 있던 문서는 그대로
4. 다시 문서 모드로 → 탭이 하나로 정리되고, 저장 안 된 문서가 있으면 물어본다
5. 앱 재시작 → 마지막 모드가 유지된다
6. **두 모드에서 각각 같은 문서를 열고 저장 → `git diff` 가 비어 있다** ← 제일 중요

---

## 10. 다크 / 라이트 전환

**모드(문서·작업실)와 완전히 별개의 축이다.** 4가지 조합이 전부 성립해야 한다.

구현 방식은 §0 과 **동일하다** — 속성 하나 + CSS. 그래서 싸다.

```
<div id="app" data-mode="doc" data-theme="light">
```

### 10.1 값 세 가지

| 값        | 뜻                      |
| -------- | ---------------------- |
| `light`  | 항상 밝게                  |
| `dark`   | 항상 어둡게                 |
| `system` | **기본값.** 운영체제 설정을 따라간다 |

`system` 일 때는 `matchMedia('(prefers-color-scheme: dark)')` 를 구독해
OS 설정이 바뀌면 **앱 재시작 없이** 즉시 따라간다.

`data-theme` 에는 항상 `light` 또는 `dark` 만 들어간다.
`system` 은 설정에만 저장되고, 화면에는 계산된 결과가 반영된다.

### 10.2 색은 CSS 변수 한 곳에서만 정의한다

```css
:root { --paper: #ffffff; --ink: #2a2724; --canvas: #e9e5dc; ... }
#app[data-theme="dark"] { --paper: #22201d; --ink: #e8e4dd; --canvas: #171614; ... }
```

**컴포넌트 CSS에 색상 값(`#fff`, `rgb(...)`)을 직접 쓰지 않는다.**
전부 `var(--...)` 를 통한다. 이걸 어기면 다크 모드에서 흰 조각이 남는다.

### 10.3 어두울 때 문서(종이)는?

**어둡게 한다.** 워드·한글의 다크 모드와 같다. 흰 종이만 밝게 남기면 눈이 아프다.

단, 다음은 **항상 밝게** 고정한다 (v0.3 내보내기까지 유지):

- PDF · HTML 내보내기 결과물
- 인쇄 미리보기

### 10.4 저장

§6 의 `settings.json` 에 같이 넣는다.

```json
{ "mode": "doc", "theme": "system" }
```

- 값이 세 가지 중 하나가 아니면 → `system` 으로 고치고 저장
- 첫 실행 화면에서는 **테마를 묻지 않는다.** 질문은 모드 하나뿐이다 (§8)

### 10.5 설정 화면

§5 의 모드 라디오 **바로 아래**에 놓는다.

```
모드     ( ● 문서    ○ 작업실 )
         한 번에 한 문서씩 종이처럼 봅니다.

화면     ( ○ 밝게    ○ 어둡게    ● 시스템 설정 )
```

즉시 적용. 확인 버튼 없음. 전환 애니메이션 없음.

### 10.6 구현 단계 (§7 에 이어서)

| 단계 | 할 일                                                     | 끝났다는 기준                                 |
| -- | ------------------------------------------------------- | --------------------------------------- |
| M6 | 색을 전부 CSS 변수로 빼고 `:root` / `[data-theme="dark"]` 두 벌 정의 | 속성을 손으로 바꾸면 전체 색이 바뀌고, 하드코딩된 색이 한 곳도 없다 |
| M7 | `theme` 설정 읽기/쓰기 + `system` 감지·구독                       | OS 설정을 바꾸면 앱이 즉시 따라간다                   |
| M8 | 설정 화면 라디오 3개                                            | 즉시 반영되고 재시작 후에도 유지된다                    |

**M6 이 M1~M5 와 독립적이다.** 병렬로 위임해도 된다.

### 10.7 완료 판정 (§9 에 추가)

7. 4가지 조합(문서·작업실 × 밝게·어둡게)이 전부 깨지지 않는다
8. `system` 으로 두고 OS 테마를 바꾸면 앱이 즉시 따라간다
9. **다크 모드에서 저장한 파일과 라이트 모드에서 저장한 파일이 동일하다**
   (테마가 데이터로 새지 않는지 확인 — §2 와 같은 이유)
