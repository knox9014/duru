# 두루 4주차 — 버전 관리

> 원본 계획서 §13 이 "이 프로젝트의 주요 차별화 요소"라고 부른 부분.
> 다만 **사용자는 Git 을 몰라야 한다.** 화면에 `commit` · `diff` · `branch`
> 같은 말이 한 번도 나오면 안 된다.

---

## 0. 이 주차의 판정 기준

이 프로젝트를 만드는 사람 본인이 **Git 을 몰랐다.**
그래서 완료 판정이 명확하다:

> **Git 을 모르는 사람이 설명 없이 "어제 뭘 고쳤는지" 볼 수 있는가.**

Git 용어가 화면에 하나라도 보이면 실패다.

---

## 1. 절대 어기면 안 되는 것

### 1.1 남의 파일을 건드리지 않는다

**`.md` 파일과 `assets/` 만 대상으로 한다.**

폴더에 코드·설정·빌드 산출물이 같이 있을 수 있다. 사용자가 문서만
고쳤다고 생각하는데 코드 변경까지 같이 저장되면 **남의 작업을 망친다.**

```
git add -- '*.md' 'assets/'      ← 이 범위 밖은 절대 건드리지 않는다
```

화면에도 "문서만 저장합니다" 를 명시한다.

### 1.2 되돌리기는 v0.1 에 넣지 않는다

기록에서 **보기만** 한다. 옛 버전으로 되돌리는 기능은 v0.2 다.

되돌리기는 지금 쓰던 내용을 날릴 수 있고, Git 을 모르는 사람에게
"되돌렸는데 되돌리기 전으로 못 돌아간다"는 상황을 만들 수 있다.
v0.2 에서 넣을 때도 **되돌리기 자체를 새 버전으로 저장**해서
이력이 사라지지 않게 한다.

### 1.3 저장소가 아니면 조용히 숨긴다

대부분의 폴더는 Git 저장소가 아니다. 그때는 **버전 관련 UI 를 전부 숨긴다.**

**`git init` 을 하지 않는다.** 사용자가 뭘 하는지 모르는 채로 `.git` 폴더가
생기면 안 된다. 타깃(개발자 옆 사람)은 **이미 누군가 만들어 둔 저장소의
문서를 고치는** 상황이다. 저장소를 만드는 건 그 사람 일이 아니다.

### 1.4 회귀 검사

```bash
npm run check
```

Git 작업은 파일 내용을 바꾸지 않아야 한다. **버전 저장 전후로
문서 바이트가 같아야 한다** — 저장(Ctrl+S)과 버전 저장은 별개다.

---

## 2. 어디에 놓나

**하단 상태바.** 우측 패널은 `MODE_PLAN.md` §3.2 에서 v0.2 로 미뤘고,
문서 모드에는 애초에 우측 패널이 없다.

```
┌──────────────────────────────────────────────────────────┐
│ 저장됨                     바뀐 문서 2개  │  기록  │ 버전 저장 │
└──────────────────────────────────────────────────────────┘
```

- 두 모드에서 **같은 자리, 같은 모양**이다 (모드 차이를 늘리지 않는다)
- 저장소가 아니면 오른쪽 세 항목이 **통째로 사라진다**
- `저장됨` 은 지금도 있는 파일 저장 상태다. 버전과 헷갈리지 않게 왼쪽에 둔다

---

## 3. G1 — 바뀐 문서 (N)

### 화면

```
바뀐 문서 2개        ← 누르면 목록이 펼쳐진다

  ┌────────────────────────────┐
  │ 기획서.md          고침     │
  │ 회의록.md          새로 만듦 │
  │                            │
  │ 문서만 저장합니다.           │
  └────────────────────────────┘
```

- 항목을 누르면 그 문서의 **변경 내용**(§4)이 열린다
- 상태는 세 가지만: `고침` · `새로 만듦` · `지움`
- 마지막 버전 이후 아무것도 안 바뀌었으면 **`바뀐 것 없음`** 을 흐리게 표시

### 내부

```
git status --porcelain -- '*.md' 'assets/'
```

- 앱이 저장할 때마다, 그리고 **5초마다** 다시 확인한다
- 큰 저장소에서 느릴 수 있다. **200ms 를 넘으면 주기를 30초로 늘린다**
  (느려서 앱이 버벅이는 것이 최악이다)

---

## 4. G2 — 변경 내용 보기

### 화면 — 모달

```
┌─ 기획서.md — 무엇이 바뀌었나 ──────────────────┐
│                                               │
│    3   ## 1. 개요                             │
│  − 4   REST API 를 사용한다.                   │
│  + 4   REST API 와 WebSocket 을 사용한다.      │
│    5                                          │
│                                               │
│                                       [ 닫기 ] │
└───────────────────────────────────────────────┘
```

- 지운 줄은 **연한 빨강 바탕에 `−`**, 넣은 줄은 **연한 초록 바탕에 `+`**
- 앞뒤 3줄을 같이 보여준다 (문맥 없이 바뀐 줄만 보면 이해가 안 된다)
- **줄 번호를 보여준다.** "몇 번째 줄" 이 사람이 문서를 찾는 단위다
- 색은 반드시 CSS 변수로. 다크에서도 읽혀야 한다

### 내부

```
git diff -U3 -- <파일>              추적 중인 파일
git diff --no-index /dev/null <파일>  새로 만든 파일
```

`@@ -3,4 +3,4 @@` 같은 헤더는 **화면에 그대로 보여주지 않는다.**
줄 번호를 계산해 표에 넣는다.

---

## 5. G3 — 버전 저장

### 화면

`버전 저장` 을 누르면:

```
┌─ 버전 저장 ────────────────────────────────┐
│                                            │
│  무엇을 바꾸셨나요?                          │
│  ┌────────────────────────────────────┐    │
│  │ 개요 절 수정                         │    │
│  └────────────────────────────────────┘    │
│                                            │
│  저장할 문서 2개                            │
│    기획서.md · 회의록.md                    │
│                                            │
│                        [ 취소 ]  [ 저장 ]   │
└────────────────────────────────────────────┘
```

- **한 줄 설명을 받는다.** 비워도 저장된다 → 그때는 `2026-08-26 14:32 수정`
- 저장될 문서 목록을 **반드시 보여준다.** 무엇이 저장되는지 모르면 안 된다
- `Enter` 로 저장, `Esc` 로 취소

### 내부

```
git add -- '*.md' 'assets/'
git commit -m "<설명>"
```

**이름·메일이 설정돼 있지 않으면 커밋이 실패한다.** 흔한 상황이다.
그때는 오류를 그대로 보여주지 말고 이렇게 안내한다:

> 버전을 저장하려면 이름이 필요합니다.
> 설정 화면에서 이름과 메일을 입력해 주세요.

→ **설정 화면에 "버전 저장에 쓸 이름 / 메일" 두 칸을 추가**하고,
   입력하면 `git config --local user.name/user.email` 로 넣는다.
   (`--local` 이다. 사용자의 전체 Git 설정을 건드리지 않는다)

### 저장 순서 — 중요

```
1. 편집 중인 문서를 먼저 파일로 저장한다 (Ctrl+S 와 같은 처리)
2. 그다음 버전 저장
```

거꾸로 하면 **방금 친 글자가 버전에 안 들어간다.** 사용자는
"저장했는데 왜 없지?" 가 된다.

---

## 6. G4 — 기록

### 화면

```
┌─ 기록 ─────────────────────────────────────┐
│                                            │
│  오늘 14:32   개요 절 수정                   │
│  오늘 13:21   표 추가                       │
│  어제 18:04   처음 작성                      │
│                                            │
│                                    [ 닫기 ] │
└────────────────────────────────────────────┘
```

- **오늘 / 어제 / 3일 전 / 2026-08-20** 처럼 사람이 읽는 시각
- 항목을 누르면 그 버전에서 **무엇이 바뀌었는지**(§4 와 같은 화면)
- 커밋 해시(`a3f9k2`)는 **보여주지 않는다.** 사람에게 아무 뜻이 없다
- 우선 **현재 문서의 기록**만. 전체 기록은 v0.2

### 내부

```
git log --format=%H%x09%at%x09%s -n 50 -- <파일>
git show --format= -U3 <해시> -- <파일>
```

---

## 7. 만들지 않는 것

- `git init` (§1.3)
- 되돌리기 (§1.2 — v0.2)
- push · pull · 원격 저장소 · 인증
- 브랜치 · 병합 · 충돌 해결
- 어떤 파일을 저장할지 고르기 (선택적 스테이징)
- 커밋 수정·되돌리기(amend/revert)
- 전체 저장소 기록 (현재 문서 것만)
- Git 용어 노출 — commit · diff · staging · HEAD · branch 전부

---

## 8. 순서

```
G1 바뀐 문서    ← 제일 작고, 나머지의 입구가 된다
G3 버전 저장    ← 이름·메일 설정이 얽혀 있다
G2 변경 내용    ← 화면이 제일 크다
G4 기록        ← G2 를 재사용한다
```

**G1 → G3 순서를 지킬 것.** 저장을 만들어야 기록에 볼 것이 생긴다.

---

## 9. 완료 조건

### 스크립트

1. `npm run check` 통과
2. 버전 저장 전후로 문서 바이트가 같다 (Git 이 내용을 안 건드린다)
3. `git status` 가 200ms 를 넘는 저장소에서 주기가 30초로 늘어난다

### 눈으로

4. 저장소가 아닌 폴더를 열면 버전 관련 UI 가 **하나도 안 보인다**
5. 문서를 고치면 상태바에 `바뀐 문서 1개` 가 뜬다
6. 눌러서 목록 → 항목 클릭 → 변경 내용이 줄 번호와 함께 보인다
7. 다크 모드에서 빨강·초록 줄이 읽힌다
8. 이름이 설정 안 된 상태로 버전 저장 → **오류 메시지가 아니라 안내**가 뜬다
9. 설정에서 이름·메일 넣고 다시 → 저장된다
10. 기록에 방금 저장한 것이 `오늘 HH:MM` 으로 보인다
11. **`.md` 가 아닌 파일을 고쳐두고 버전 저장 → 그 파일은 저장되지 않는다**
12. 편집 중인 글을 저장 안 한 채로 버전 저장 → **그 내용까지 들어간다** (§5)

### 사람으로 — 진짜 판정

13. **Git 을 모르는 사람에게 아무 설명 없이 이렇게 시킨다:**
    "문서 한 줄 고치고, 버전으로 저장하고, 뭐가 바뀌었는지 보여주세요."
    해내면 통과. 화면에서 Git 용어를 하나라도 발견하면 실패.

---

## 10. 막히면

- Rust 에서 `git` 을 부르는 방법은 이미 있다 (1주차에 시스템 `git` 을 쓰기로 정함).
  `std::process::Command` 로 실행하고 stdout 을 문자열로 넘긴다
- 한글 파일명 → `git config core.quotepath false` 를 읽기 명령에 붙인다.
  안 그러면 `\355\225\234` 같은 8진수로 나온다
- 작업 폴더는 **열려 있는 워크스페이스 루트**다. 그 위에 `.git` 이 있는지는
  `git rev-parse --show-toplevel` 로 확인한다
- 판단이 필요한 결정이 나오면 **추측하지 말고 멈추고 물어본다**
